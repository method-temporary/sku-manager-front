import { LearningStateCount } from '../../student/model/vo/LearningStateCount';

export class TrainingCountModel {
  learningStateCount: LearningStateCount = new LearningStateCount();
  totalStudentCount: number = 0;
}
